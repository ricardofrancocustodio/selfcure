# selfcure
Selfcure — Self-healing test automation CLI. Crawls your frontend code, maps elements by reliability (data-testid → id → name → CSS → XPath), generates Playwright tests, runs them, and automatically fixes flaky locators. Knows the difference between a bad test and a bad component.
