{
  "name": "selfcure-root",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered self-healing test suite using Playwright + Claude — monorepo root",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/crawler && npm run build -w packages/analyzer && npm run build -w packages/generator && npm run build -w packages/runner && npm run build -w packages/selfcure && npm run build -w packages/reporter && npm run build -w packages/web && npm run build -w packages/mcp && npm run build -w packages/cli",
    "test": "vitest",
    "lint": "tsc --noEmit"
  },
  "keywords": ["playwright", "ai", "self-healing", "testing", "claude"],
  "author": "ricardofrancocustodio",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "latest",
    "tsup": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
