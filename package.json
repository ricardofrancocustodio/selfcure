{
  "name": "selfcure-root",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered self-healing test suite using Playwright + Claude — monorepo root",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest",
    "lint": "tsc --noEmit"
  },
  "keywords": ["playwright", "ai", "self-healing", "testing", "claude"],
  "author": "ricardofrancocustodio",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "latest",
    "tsup": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
