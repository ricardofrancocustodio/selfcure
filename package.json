{
  "name": "selfcure-root",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend testability maturity for test automation — scores components, detects ambiguous locators, and ships data-testid fixes as a PR before Cypress, Playwright, Selenium, TestCafe, or WebdriverIO run. Monorepo root.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/crawler && npm run build -w packages/analyzer && npm run build -w packages/generator && npm run build -w packages/runner && npm run build -w packages/selfcure && npm run build -w packages/reporter && npm run build -w packages/web && npm run build -w packages/mcp && npm run build -w packages/cli",
    "test": "vitest",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "test-automation",
    "testability",
    "e2e",
    "e2e-testing",
    "data-testid",
    "selectors",
    "locators",
    "playwright",
    "cypress",
    "selenium",
    "testcafe",
    "webdriverio",
    "accessibility",
    "wcag",
    "qa",
    "ai"
  ],
  "author": "ricardofrancocustodio",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "latest",
    "tsup": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
